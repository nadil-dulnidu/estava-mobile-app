services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: estava-backend
    restart: unless-stopped
    ports:
      - "5001:5001"
    env_file:
      - ./backend/.env

# Intentionally no MongoDB service.
# This project must use external MongoDB Atlas per assignment rules.
